# Logs directory

This directory is reserved for future server-side logs.

Today the site is static, so runtime logs are stored in the browser (localStorage).
You can export them from the admin page:

- `/pages/leads.html` -> "Export logs"

Log key: `brazildecoded_logs`
